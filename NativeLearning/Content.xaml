<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="NativeLearning.Content"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:NativeLearning"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        
       
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
           
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                
            </Grid.ColumnDefinitions>


            <StackPanel VerticalAlignment="Center" Grid.Column="0" Margin="10">
                <Image Height="200" Source="{x:Bind podcastData.url}" Margin="5"  Grid.Row="0" Stretch="Uniform"/>
            </StackPanel>

            <StackPanel Grid.Column="1"  Margin="10,20,0,10" HorizontalAlignment="Center" VerticalAlignment="Center">

                
              <TextBlock Text="{x:Bind podcastData.name}" Margin="5" FontSize="40"/>
                <TextBlock FontSize="20" x:Name="desc">
                    
                </TextBlock>

            </StackPanel>
            
            

        </Grid>

        <Button Grid.Row="1" Click="Button_Click"> Go Back</Button>

        

        

        

    </Grid>
</Page>
